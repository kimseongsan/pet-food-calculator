# 프로젝트 결정사항 기록

반려동물 사료량 계산기 프로젝트에서 지금까지 논의·확정된 사항을 정리한 문서입니다.
새로운 결정이 생기면 이 문서에 이어서 추가해주세요.

## 1. 기술 스택

- 순수 HTML + CSS + Vanilla JavaScript. 빌드 도구(webpack, vite 등) 없이 정적 파일을 그대로
  서빙하는 구조를 유지한다.
- 이유: 로딩 속도가 SEO·애드센스 승인에 유리하고, 배포 난이도가 낮음(Vercel/Netlify/GitHub Pages
  어디에도 바로 배포 가능).
- 외부 CDN 라이브러리는 최소화. 현재는 Google Fonts(Noto Sans KR)만 사용.

## 2. 계산 로직

- DER(일일 에너지 요구량) = RER(70 × 체중kg^0.75) × 생애주기별 계수.
- 계수는 WSAVA(세계소동물수의사회)·AAHA(미국동물병원협회) 반려동물 영양 가이드라인을 참고한
  근사치이며, [js/calculator.js](js/calculator.js) 주석에 산출 근거를 남겨두었다.
- 생애주기 경계값: 강아지는 4개월/12개월/7세(84개월), 고양이는 4개월/12개월/10세(120개월) 기준.
- 상세모드에서는 활동량·중성화 여부·임신/수유 상태를 반영해 계수를 재계산한다(간편모드는 활동량
  "보통", 중성화 "완료"로 고정 가정).
- 계산 결과는 참고용이며 정확한 급여량은 수의사 상담이 필요하다는 문구를 계산기 전반에 상시 노출한다.

## 3. 스테이지별 로드맵 (진행 순서)

1. **1차 스테이지 (완료)**: 정적 계산기 + SEO 페이지(FAQ/블로그/법적 페이지) + 실제 도메인 배포.
   외부 계정 없이 코드만으로 완결되는 범위.
2. **2차 스테이지 (보류, 코드 작업 아님)**: 쿠팡파트너스·구글 애드센스 등 외부 플랫폼 신청/승인.
   트래픽이 어느 정도 생긴 뒤 진행.
3. **3차 스테이지 (착수 조건 충족 시)**: 회원가입, 사료 데이터베이스 검색, 사용자 제보 크라우드소싱 등.
   Supabase 연동이 필요하며, "상세모드 이탈률이 높다" 등 실제 데이터 기반 신호가 있을 때 착수하기로
   결정. 트래픽이 생겼다는 이유만으로는 착수하지 않는다.

## 4. UI/디자인 결정 이력

### v1 (최초 버전)
- 웜톤 파스텔 팔레트(오렌지·크림), 손그림 느낌의 카드형 UI.
- 사료량 시각화: 컵/숟가락 SVG 안에 사료 모양(원·별·하트·삼각형·막대)을 사용자가 선택해 흩뿌려
  보여주는 방식.

### v2 (현재 버전, 2026-08-21 변경)
- **디자인 방향**: 국내 펫커머스 앱(팻프렌즈류)에서 흔한 화이트 베이스 + 코랄 포인트 컬러, 카드형
  UI, 필(pill) 형태 세그먼트 칩, 굵은 타이포그래피로 전면 개편.
  - 배경: 웜 오프화이트(`#f7f6f4`) + 화이트 카드(`#ffffff`)
  - 포인트 컬러: 코랄(`#ff5a3c`), 보조 민트 액센트(`#2ec4b6`)
  - 버튼/칩: 필 배경(`#f4f3f1`) 위에 흰색 활성 탭 방식의 세그먼트 컨트롤로 변경
- **시각화 변경**: 사료 모양 흩뿌리기(별/하트/삼각형 등)를 완전히 제거하고, 컵·숟가락을 그라데이션
  질감(케이블 톤 tan→brown 그라데이션 + 하이라이트 타원)으로 채워 더 자연스러운 일러스트로 교체.
  숟가락은 스테인리스 느낌의 메탈릭 그라데이션 적용.
- **결과 문구 단순화**: 기존의 다줄짜리 기술적 설명(정확한 분수 표기 + 여러 줄의 주의문구)을
  "하루 OOg (컵으로는 약 3/4컵) 정도 주시면 돼요" 형태의 한 줄 요약 문장 + 짧은 보조 설명 한 줄로
  축약. `#visual-caption-main`(주 문장) / `#visual-caption-sub`(보조 문구) 구조로 분리.
- 참고: petfriends.co.kr 실제 접속은 네트워크 제약으로 확인하지 못했고, 일반적인 국내 펫커머스
  앱들의 디자인 언어(화이트+포인트컬러, 카드형 UI)를 참고해 재구성한 것임. 브랜드 로고·자산을
  그대로 가져오지 않았다.

## 5. 백로그 (아이디어 메모, 미착수)

손글씨 기획 메모(2026-08-21)에서 나온 아이디어 중 아직 구현하지 않은 항목들. 3차 스테이지 이후
우선순위 논의 대상으로 남겨둔다.

- 현재 사용 중인 사료를 입력하면 Claude AI 웹서치로 정보를 조회해 보여주는 기능
- 급여 알림(캘린더/푸시)과 사료 소진일 연동 강화
- 개통(반려동물 등록) 시점마다 체크 + 히스토리 그래프(체중 변화 추이 등)
- 간식 등록 기능(원재료·칼로리 별도 관리)
- 사료 이름 직접 검색 입력(현재는 "준비 중" 상태의 비활성 입력창으로 자리만 마련해둠 — 3차
  스테이지 9단계 Supabase `foods` 테이블 연동 시 활성화 예정)
- PDF/캡처 붙여넣기로 사료 정보 등록하는 스터디形 입력 방식

## 6. 알려진 이슈 / 참고사항

- 로컬 정적 서버로 미리보기 테스트 시, 같은 포트를 재사용하면 브라우저(또는 프리뷰 인프라)의 캐시로
  인해 수정된 HTML/JS가 반영되지 않는 현상이 있었다. `.claude/launch.json`의 포트를 바꾸거나
  서버를 완전히 재시작(stop → start)하면 해결된다.
- `privacy.html`/`terms.html`은 법률 자문이 아닌 초안이며, 실제 서비스 운영 전 전문가 검토가
  필요하다(코드 내 TODO 주석으로 표시됨).
